#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int N = 3000 + 5;
const int MOD = 998244353;
int n, s, a[N];

void Main() {
    cin >> n >> s;
    for(int i = 0; i < n; i++)
        cin >> a[i];
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    int t = 1;
    // cin >> t;
    while (t--)
        Main();
    return 0;
}
